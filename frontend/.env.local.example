# Local overrides (copy to .env.local — takes precedence over .env)
# Use this when developing against a local backend instead of Render.
VITE_API_URL=http://localhost:5001/api

# Or proxy via Vite (no CORS): set VITE_API_URL=/api and run backend on :5001
